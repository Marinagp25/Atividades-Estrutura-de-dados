/*#include <stdio.h>

int contarOcorrencias(int vetor[], int tamanho, int n) {
    int contador = 0;
    for (int i = 0; i < tamanho; i++) {
        if (vetor[i] == n) {
            contador++;
        }
    }
    return contador;
}

int main() {
    int vetor[10];
    int n;

    for (int i = 0; i < 10; i++) {
        printf("Digite o número %d: ", i + 1);
        scanf("%d", &vetor[i]);
    }

    printf("Digite o número a ser verificado: ");
    scanf("%d", &n);

    int ocorrencias = contarOcorrencias(vetor, 10, n);

    printf("O número %d ocorre %d vezes no vetor.\n", n, ocorrencias);

    return 0;
}*/
