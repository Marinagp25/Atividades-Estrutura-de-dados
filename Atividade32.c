/*#include <stdio.h>
#include <ctype.h>

void transformaMinusculas(char *string) {
    int i = 0;
    while (string[i]) {
        string[i] = tolower(string[i]);
        i++;
    }
}

int main() {
    char string[100];

    printf("Digite uma string: ");
    gets(string);

    transformaMinusculas(string);

    printf("A string em minúsculas: %s\n", string);

    return 0;
}*/
