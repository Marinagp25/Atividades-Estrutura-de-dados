/*#include <stdio.h>

int main() {
    int vetor[12];
    int x, y;

    printf("Digite os valores do vetor (12 posições):\n");
    for (int i = 0; i < 12; i++) {
        scanf("%d", &vetor[i]);
    }

    printf("Digite o valor de X (entre 0 e 11): ");
    scanf("%d", &x);

    printf("Digite o valor de Y (entre 0 e 11): ");
    scanf("%d", &y);

    if (x >= 0 && x < 12 && y >= 0 && y < 12) {
        int soma = vetor[x] + vetor[y];
        printf("A soma dos valores nas posições %d e %d é: %d\n", x, y, soma);
    } else {
        printf("Valores de X e Y estão fora dos limites do vetor.\n");
    }

    return 0;
}*/
