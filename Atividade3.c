/*#include <stdio.h>

int main() {

    int quantidadeAlunos = 15;
    float notasProva1, notasProva2, media;

    for (int aluno = 1; aluno <= quantidadeAlunos; aluno++) {
        printf("Digite a nota da primeira prova do aluno %d: ", aluno);
        scanf("%f", &notasProva1);

        printf("Digite a nota da segunda prova do aluno %d: ", aluno);
        scanf("%f", &notasProva2);

        media = (notasProva1 + notasProva2) / 2;

        if (media >= 6.0) {
            printf("O aluno %d foi Aprovado\n", aluno);
        } else {
            printf("O aluno %d foi Reprovado\n", aluno);
        }
    }

    return 0;
}*/
