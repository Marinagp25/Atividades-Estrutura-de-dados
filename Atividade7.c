/*#include <stdio.h>

int main() {
    int tamanho = 10;
    double vetor[tamanho];
    double numeroInicial, razao;

    printf("Digite o número inicial da P.G.: ");
    scanf("%lf", &numeroInicial);

    printf("Digite a razão da P.G.: ");
    scanf("%lf", &razao);

    for (int i = 0; i < tamanho; i++) {
        vetor[i] = numeroInicial * pow(razao, i);
    }

    printf("Os termos da P.G. são:\n");
    for (int i = 0; i < tamanho; i++) {
        printf("%.2lf ", vetor[i]);
    }
    printf("\n");

    return 0;
}*/
