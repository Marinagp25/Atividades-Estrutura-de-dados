/*#include <stdio.h>
#include <ctype.h>

void transformaMaiusculas(char *string) {
    int i = 0;
    while (string[i]) {
        string[i] = toupper(string[i]);
        i++;
    }
}

int main() {
    char string[100];

    printf("Digite uma string: ");
    gets(string); 

    transformaMaiusculas(string); 

    printf("A string em maiúsculas: %s\n", string);

    return 0;
}*/
