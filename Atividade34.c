/*#include <stdio.h>

void substituiCaracterePorAsterisco(char *string, char caractere) {
    int i = 0;

    while (string[i] != '\0') {
        if (string[i] == caractere) {
            string[i] = '*';
        }
        i++;
    }
}

int main() {
    char string[100];
    char caractere;

    printf("Digite uma string: ");
    gets(string); 
  
    printf("Digite um caractere para substituir: ");
    scanf(" %c", &caractere); 

    substituiCaracterePorAsterisco(string, caractere);

    printf("String após a substituição: %s\n", string);

    return 0;
}*/
